# test_readme

Проект `test_readme` - это минимальный проект на Rust, содержащий два файла в директории `src`: `greeter.rs` и `user.rs`. Файл `greeter.rs` содержит функцию `greet`, которая приветствует переданное ей имя. Файл `user.rs` определяет структуру `User` с полями `name` и `age`, а также реализует метод `new` для создания новой экземпляра `User` и метод `Display` для форматирования вывода пользователя.

Проект использует `Cargo.toml` в качестве файла конфигурации для управления зависимостями и сборки проекта. Также в проекте присутствует файл `.gitignore`, который определяет игнорируемые файлы и директории.

Ниже представлены файлы проекта:

- `src/greeter.rs`
- `src/user.rs`
- `.gitignore`
- `Cargo.toml`

Проект не содержит исходного кода, только конфигурационные файлы и файлы сборки.

<!-- readme-ai: 401efeaeb925ef784153696b720ea3b6f5863b84 -->